import SignOutButton from "@/components/sign-out-button";
import React from "react";

const Home = () => {
    return (
        <div>
            Home
            <SignOutButton />
        </div>
    );
};

export default Home;
